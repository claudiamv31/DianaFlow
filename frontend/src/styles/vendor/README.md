# Vendor styles

Shared overrides for third-party DOM live here. Keep selectors scoped beneath a DianaFlow-owned root class, and register each stylesheet in `styling-contract.json`.

Existing co-located `react-calendar` overrides remain registered as legacy files until their calendar variants can be consolidated without a visual redesign.
